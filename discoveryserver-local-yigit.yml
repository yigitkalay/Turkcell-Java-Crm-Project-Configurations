server:
  # This field is customizable
  port: 8888
eureka:
  client:
    serviceUrl:
      # This field port section is customizable.
      defaultZone: http://localhost:${server.port}/eureka/
    registerWithEureka: false
    fetchRegistry: false
